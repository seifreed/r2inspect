# Quick Task {number}: {objective}

## Objective
- **What:** {objective}
- **Why:** Complete the quick task with deterministic intake and verification.
- **Output:** `.planning/quick/{number}-{slug}/{number}-PLAN.md` with measurable checks.

## Steps
1. Validate quick preflight (`roadmap_exists`, `planning_exists`, `task_dir`).
2. Create normalized task directory `.planning/quick/{number}-{slug}`.
3. Execute task and update summary evidence when completed.

## Verification
{checks}
