---
phase: 02-milestone-governance-gates
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/governance_gates.py
  - tests/unit/test_governance_gates.py
  - .planning/v1.1-MILESTONE-AUDIT.md
autonomous: true
requirements:
  - GOV-01
  - GOV-02
must_haves:
  truths:
    - "El gate detecta y bloquea completion cuando falta `.planning/vX.Y-MILESTONE-AUDIT.md`."
    - "El gate detecta y bloquea completion cuando `status` no es exactamente `passed`."
    - "El gate clasifica fallos por tipo (`missing_file`, `invalid_status`, `stale_audit`, `malformed_sections`) con salida determinista."
  artifacts:
    - path: "scripts/governance_gates.py"
      provides: "Motor central para evaluar gate de gobernanza de milestone."
      exports: ["evaluate_milestone_governance_gate", "format_gate_failures"]
    - path: "tests/unit/test_governance_gates.py"
      provides: "Cobertura automatizada de rutas fail-closed y parseo de audit artifact."
      contains: "test_gate_fails_when_audit_file_missing"
    - path: ".planning/v1.1-MILESTONE-AUDIT.md"
      provides: "Contrato canonico del audit artifact para el milestone activo."
      contains: "status: passed"
  key_links:
    - from: "scripts/governance_gates.py"
      to: ".planning/v1.1-MILESTONE-AUDIT.md"
      via: "resolucion de ruta canonica + parseo de frontmatter"
      pattern: "v\\d+\\.\\d+-MILESTONE-AUDIT\\.md|status"
    - from: "scripts/governance_gates.py"
      to: ".planning/STATE.md"
      via: "lectura de timestamp para staleness"
      pattern: "last_updated|stale"
    - from: "scripts/governance_gates.py"
      to: ".planning/ROADMAP.md"
      via: "comparacion de mtime para coherencia del audit"
      pattern: "ROADMAP\\.md|stale"
---

<objective>
Crear el motor de gobernanza fail-closed para milestone, con contrato de auditoria canonico y clasificacion determinista de fallos.

Purpose: Establecer una sola fuente de verdad para checks GOV-01/GOV-02 antes de integrar comandos de completion.
Output: `scripts/governance_gates.py` + pruebas unitarias + artifact de auditoria canonico para v1.1.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-milestone-governance-gates/02-CONTEXT.md
@.planning/phases/02-milestone-governance-gates/02-RESEARCH.md
@scripts/quick_bootstrap.py

<interfaces>
From `scripts/quick_bootstrap.py`:

```python
def close_quick_task(...): ...
def parse_args() -> argparse.Namespace: ...
```

Gate contracts to create in this plan:

```python
def evaluate_milestone_governance_gate(planning_root: Path, milestone_version: str) -> dict[str, object]:
    ...

def format_gate_failures(result: dict[str, object], retry_command: str) -> str:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement canonical audit contract parsing and fail-closed status checks</name>
  <files>scripts/governance_gates.py, tests/unit/test_governance_gates.py, .planning/v1.1-MILESTONE-AUDIT.md</files>
  <behavior>
    - Test 1: cuando falta `.planning/v1.1-MILESTONE-AUDIT.md`, el resultado devuelve `passed=false` con codigo `missing_file`.
    - Test 2: cuando `status` no es `passed` (o falta), el resultado devuelve `passed=false` con codigo `invalid_status`.
    - Test 3: si faltan secciones requeridas del documento de auditoria, el resultado devuelve `passed=false` con codigo `malformed_sections`.
  </behavior>
  <action>Crear `scripts/governance_gates.py` con una funcion `evaluate_milestone_governance_gate` que resuelva la ruta canonica `.planning/vX.Y-MILESTONE-AUDIT.md`, lea frontmatter/sections requeridas y aplique fail-closed. Crear `tests/unit/test_governance_gates.py` con fixtures de audit validas e invalidas. Crear el artifact base `.planning/v1.1-MILESTONE-AUDIT.md` con frontmatter inicial y secciones requeridas para que el contrato quede explicito desde esta fase.</action>
  <verify>
    <automated>pytest -q tests/unit/test_governance_gates.py -k "missing or status or malformed" -x</automated>
  </verify>
  <done>El gate identifica de forma determinista ausencia de audit, status invalido y contrato malformed sin mutar estado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add staleness detection and grouped deterministic failure envelope</name>
  <files>scripts/governance_gates.py, tests/unit/test_governance_gates.py</files>
  <behavior>
    - Test 1: si `ROADMAP.md` o `STATE.md` son mas recientes que el timestamp auditado, retorna `stale_audit`.
    - Test 2: multiples fallos aparecen agrupados por tipo y en orden determinista.
    - Test 3: `format_gate_failures` siempre incluye checklist accionable y comando de retry.
  </behavior>
  <action>Extender el evaluador para comparar timestamp auditado contra `STATE.md:last_updated` y mtime de `ROADMAP.md`. Definir envelope de salida estable (`passed`, `failure_groups`, `retry_command`) y formatter para reutilizar en precheck/completion. Mantener severidad solo `error` en esta fase y evitar cualquier modo bypass.</action>
  <verify>
    <automated>pytest -q tests/unit/test_governance_gates.py -k "stale or grouped or retry" -x</automated>
  </verify>
  <done>El evaluador central devuelve fallos agrupados, deterministas y listos para surfaced UX en completion flow.</done>
</task>

</tasks>

<verification>
- Ejecutar `pytest -q tests/unit/test_governance_gates.py -x` y confirmar cobertura de rutas missing/status/stale/malformed.
- Validar que `scripts/governance_gates.py` no realiza mutaciones de `.planning/*` durante evaluacion (solo lectura).
- Verificar que el formatter genera checklist con un unico comando de retry copy-paste.
</verification>

<success_criteria>
- Existe un evaluador unico de gate reutilizable por precheck y completion.
- GOV-01 y GOV-02 quedan cubiertos por pruebas automatizadas fail-closed.
- La salida de fallos queda estructurada por tipo y lista para integracion CLI.
</success_criteria>

<output>
After completion, create `.planning/phases/02-milestone-governance-gates/02-01-SUMMARY.md`
</output>
