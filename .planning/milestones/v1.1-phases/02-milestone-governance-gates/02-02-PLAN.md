---
phase: 02-milestone-governance-gates
plan: 2
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - scripts/quick_bootstrap.py
  - scripts/governance_gates.py
  - tests/unit/test_quick_bootstrap.py
  - tests/integration/test_milestone_governance_flow.py
  - .planning/STATE.md
autonomous: true
requirements:
  - GOV-01
  - GOV-02
  - GOV-03
must_haves:
  truths:
    - "El operador puede ejecutar un precheck no bloqueante antes de cerrar milestone."
    - "El completion de milestone aborta fail-closed cuando el gate no pasa, sin avanzar estado ni historico."
    - "El bloqueo muestra checklist de remediacion agrupado por tipo y siempre incluye un comando de reintento determinista."
  artifacts:
    - path: "scripts/quick_bootstrap.py"
      provides: "Entrypoints locales para `milestone precheck` y `milestone complete` con gate obligatorio."
      exports: ["parse_args", "main"]
    - path: "tests/integration/test_milestone_governance_flow.py"
      provides: "Pruebas de flujo end-to-end del gate en precheck y completion."
      contains: "test_completion_aborts_without_state_mutation_on_gate_failure"
    - path: "tests/unit/test_quick_bootstrap.py"
      provides: "Cobertura de CLI routing y salida checklist/retry."
      contains: "test_milestone_precheck_reports_grouped_failures"
    - path: ".planning/STATE.md"
      provides: "Registro de intento de completion/precheck sin mutar estados finales en fallo."
      contains: "Last activity"
  key_links:
    - from: "scripts/quick_bootstrap.py"
      to: "scripts/governance_gates.py"
      via: "invocacion del evaluador compartido antes de completion"
      pattern: "evaluate_milestone_governance_gate|milestone precheck|milestone complete"
    - from: "scripts/quick_bootstrap.py"
      to: ".planning/STATE.md"
      via: "escritura de evidencia solo en rutas permitidas, sin cierre en fallo"
      pattern: "Last activity|close_quick_task"
    - from: "tests/integration/test_milestone_governance_flow.py"
      to: "scripts/quick_bootstrap.py"
      via: "ejecucion CLI de precheck/completion en workspace temporal"
      pattern: "bootstrap main|milestone"
---

<objective>
Integrar el gate en la frontera real de completion con una ruta de precheck y salida de remediacion accionable.

Purpose: Cumplir GOV-03 y cerrar la brecha operativa entre evaluacion del gate y comando de cierre de milestone.
Output: Comandos de precheck/complete cableados al gate central, pruebas E2E de fail-closed y salida checklist con retry.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-milestone-governance-gates/02-CONTEXT.md
@.planning/phases/02-milestone-governance-gates/02-RESEARCH.md
@.planning/phases/02-milestone-governance-gates/02-01-PLAN.md
@scripts/quick_bootstrap.py
@scripts/governance_gates.py

<interfaces>
From `scripts/governance_gates.py` (Plan 02-01):

```python
def evaluate_milestone_governance_gate(planning_root: Path, milestone_version: str) -> dict[str, object]: ...
def format_gate_failures(result: dict[str, object], retry_command: str) -> str: ...
```

CLI shape to enforce in this plan:

```bash
python scripts/quick_bootstrap.py milestone precheck v1.1
python scripts/quick_bootstrap.py milestone complete v1.1
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add milestone precheck and completion gate routing to local CLI</name>
  <files>scripts/quick_bootstrap.py, tests/unit/test_quick_bootstrap.py, tests/integration/test_milestone_governance_flow.py</files>
  <behavior>
    - Test 1: `milestone precheck v1.1` ejecuta el gate y devuelve exit code no bloqueante con resultado estructurado.
    - Test 2: `milestone complete v1.1` aborta antes de cualquier mutacion cuando gate falla.
    - Test 3: si gate pasa, la ruta de completion continua normalmente y deja evidencia de cierre.
  </behavior>
  <action>Extender `parse_args`/`main` en `scripts/quick_bootstrap.py` con subcomandos `milestone precheck` y `milestone complete`, ambos consumiendo el evaluador compartido de `scripts/governance_gates.py`. En `complete`, ejecutar gate al inicio y retornar fallo inmediato sin tocar archivos de archivo/historial cuando `passed=false`. Agregar pruebas unitarias de routing y una prueba de integracion para verificar abort-before-mutation.</action>
  <verify>
    <automated>pytest -q tests/unit/test_quick_bootstrap.py -k "milestone and (precheck or complete)" -x && pytest -q tests/integration/test_milestone_governance_flow.py -x</automated>
  </verify>
  <done>El flujo de completion queda protegido por gate fail-closed y existe una ruta de readiness precheck reusable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface grouped remediation checklist with deterministic retry guidance</name>
  <files>scripts/quick_bootstrap.py, scripts/governance_gates.py, tests/unit/test_quick_bootstrap.py, tests/integration/test_milestone_governance_flow.py</files>
  <behavior>
    - Test 1: la salida de fallo agrupa errores por tipo (`missing_file`, `invalid_status`, `stale_audit`, `malformed_sections`).
    - Test 2: la salida siempre incluye pasos de remediacion concretos por grupo.
    - Test 3: la salida siempre incluye un unico comando de retry copy-paste (`milestone precheck <version>` o `milestone complete <version>`).
  </behavior>
  <action>Usar `format_gate_failures` para imprimir checklist determinista con remediation steps y retry command obligatorio, respetando modelo de severidad solo error de la fase 2. Estandarizar salida para que precheck y complete usen la misma clasificacion y texto base, cambiando solo el comando de reintento recomendado segun contexto.</action>
  <verify>
    <automated>pytest -q tests/unit/test_quick_bootstrap.py -k "grouped_failures or retry_command" -x && pytest -q tests/integration/test_milestone_governance_flow.py -k "remediation" -x</automated>
  </verify>
  <done>Operador recibe diagnostico accionable con checklist y comando determinista para reintentar sin ambiguedad.</done>
</task>

<task type="auto">
  <name>Task 3: Record gate-attempt evidence without false completion advancement</name>
  <files>scripts/quick_bootstrap.py, tests/integration/test_milestone_governance_flow.py, .planning/STATE.md</files>
  <action>Ajustar registro en `STATE.md` para reflejar intentos de precheck/completion y resultado del gate sin marcar milestone como completado cuando hay fallo. Añadir prueba de no-regresion que valide: fallo de gate mantiene estado de milestone intacto, y exito permite continuar con flujo normal de cierre.</action>
  <verify>
    <automated>pytest -q tests/integration/test_milestone_governance_flow.py -k "state_mutation or blocked_completion" -x && rg -n "Last activity" .planning/STATE.md</automated>
  </verify>
  <done>El historial operativo refleja intentos de gate y no reporta cierres falsos cuando el gate bloquea completion.</done>
</task>

</tasks>

<verification>
- Ejecutar suite unitaria/integracion de milestone governance y confirmar bloqueo temprano en errores.
- Simular `milestone precheck` con audit faltante y verificar checklist + retry command.
- Simular `milestone complete` con gate fallido y confirmar que no hay mutaciones de cierre.
</verification>

<success_criteria>
- Precheck y completion usan el mismo evaluador y misma taxonomia de fallos.
- Completion nunca avanza cuando audit falta, status es invalido o audit esta stale.
- GOV-03 queda cubierto con salida de remediacion accionable y reintento determinista.
</success_criteria>

<output>
After completion, create `.planning/phases/02-milestone-governance-gates/02-02-SUMMARY.md`
</output>
