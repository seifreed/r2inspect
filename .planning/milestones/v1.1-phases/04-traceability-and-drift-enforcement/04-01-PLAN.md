---
phase: 04-traceability-and-drift-enforcement
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/governance_gates.py
  - tests/unit/test_governance_gates.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - TRC-01
  - TRC-02
must_haves:
  truths:
    - "Every active requirement (v1+v2) maps to exactly one canonical roadmap phase id."
    - "Traceability drift failures are emitted in stable, deterministic group order."
    - "Cross-artifact checks fail when mapping references unknown phases or malformed traceability rows."
  artifacts:
    - path: "scripts/governance_gates.py"
      provides: "Shared traceability/drift evaluator plus deterministic failure formatter hooks."
      exports: ["evaluate_traceability_drift_gate", "_parse_traceability_rows", "_normalize_phase_id"]
    - path: "tests/unit/test_governance_gates.py"
      provides: "Unit coverage for parse contracts, one-to-one mapping, and failure ordering."
      contains: "test_traceability_gate_detects_unmapped_and_multi_phase_active_requirements"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Canonical source for requirement->phase mapping via Traceability table."
      contains: "## Traceability"
    - path: ".planning/ROADMAP.md"
      provides: "Canonical set of known phases and completion markers for drift comparison."
      contains: "### Phase 4: Traceability and Drift Enforcement"
  key_links:
    - from: "scripts/governance_gates.py"
      to: ".planning/REQUIREMENTS.md"
      via: "Traceability table parser and active requirement scope resolver"
      pattern: "Requirement | Phase | Status"
    - from: "scripts/governance_gates.py"
      to: ".planning/ROADMAP.md"
      via: "Phase catalog normalization and unknown-phase rejection"
      pattern: "Phase 2|Phase 3|Phase 4"
    - from: "tests/unit/test_governance_gates.py"
      to: "scripts/governance_gates.py"
      via: "Asserts deterministic failure group order and canonical normalization"
      pattern: "missing_file|malformed_traceability_table|unmapped_requirement|multi_phase_mapping"
---

<objective>
Build the shared traceability and drift evaluator in `scripts/governance_gates.py` so active requirement mappings are one-to-one and normalized against roadmap phases.

Purpose: Establish the single source of truth and deterministic failure taxonomy required by TRC-01/TRC-02 before transition wiring.
Output: Parser + evaluator + formatter-ready envelope with strict ordering, and unit tests proving the contract.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-traceability-and-drift-enforcement/04-CONTEXT.md
@.planning/phases/04-traceability-and-drift-enforcement/04-RESEARCH.md
@scripts/governance_gates.py
@tests/unit/test_governance_gates.py

<interfaces>
From `scripts/governance_gates.py`:

```python
def evaluate_requirements_contract_gate(...): ...
def format_requirements_contract_failures(...): ...
```

New contract introduced in this plan:

```python
def evaluate_traceability_drift_gate(
    planning_root: Path,
    *,
    scope: str = "all",
    touched_requirement_ids: set[str] | None = None,
) -> dict[str, Any]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical phase normalization and traceability-table parser with strict structure checks</name>
  <files>scripts/governance_gates.py, tests/unit/test_governance_gates.py</files>
  <behavior>
    - Test 1: `Phase 4`, `4`, and `04` normalize to the same canonical phase token.
    - Test 2: malformed or missing `## Traceability` table yields `malformed_traceability_table`.
    - Test 3: active requirement scope includes only v1/v2 requirements, excluding Out of Scope.
  </behavior>
  <action>Implement helper parsers for roadmap phase catalog and requirements traceability rows with canonical phase normalization (`2`, `3`, `4`, `2.1`). Add deterministic malformed-table diagnostics for missing headers, short rows, and blank requirement/phase cells. Keep all parsing read-only and pure.</action>
  <verify>
    <automated>pytest -q tests/unit/test_governance_gates.py -k "traceability and (normalize or malformed_table or active_scope)" -x</automated>
  </verify>
  <done>Core parse model exists and rejects malformed traceability input deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement one-to-one mapping and unknown-phase drift checks in shared evaluator</name>
  <files>scripts/governance_gates.py, tests/unit/test_governance_gates.py</files>
  <behavior>
    - Test 1: active requirement without mapping fails with `unmapped_requirement`.
    - Test 2: active requirement mapped to multiple phases fails with `multi_phase_mapping`.
    - Test 3: mapping to a phase not present in roadmap fails with `unknown_mapped_phase`.
  </behavior>
  <action>Add `evaluate_traceability_drift_gate(..., scope="all")` that enforces exactly-one mapping for every active requirement and validates mapped phases against roadmap phase catalog. Return stable envelope fields (`passed`, `failure_groups`, `retry_command`, `scope`, `touched_requirement_ids`) and canonical group order: `missing_file`, `malformed_traceability_table`, `missing_touched_requirements`, `unknown_touched_requirement`, `unmapped_requirement`, `multi_phase_mapping`, `unknown_mapped_phase`, `state_mapping_mismatch`.</action>
  <verify>
    <automated>pytest -q tests/unit/test_governance_gates.py -k "traceability and (unmapped or multi_phase or unknown_mapped_phase or ordering)" -x</automated>
  </verify>
  <done>Evaluator enforces TRC-01 hard errors and returns deterministic failure ordering.</done>
</task>

</tasks>

<verification>
- Run `pytest -q tests/unit/test_governance_gates.py -k "traceability" -x`.
- Confirm evaluator output is deterministic across two consecutive runs with identical fixtures.
- Confirm no writes to `.planning/*` occur from evaluator execution paths.
</verification>

<success_criteria>
- TRC-01 is covered by strict one-to-one active requirement mapping enforcement.
- TRC-02 foundational checks exist for malformed/missing/unknown mapping drift classes.
- A single shared evaluator contract exists for later CLI wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/04-traceability-and-drift-enforcement/04-01-SUMMARY.md`
</output>
