---
phase: 04-traceability-and-drift-enforcement
plan: 2
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - scripts/quick_bootstrap.py
  - scripts/governance_gates.py
  - tests/unit/test_quick_bootstrap.py
  - .planning/STATE.md
autonomous: true
requirements:
  - TRC-02
  - TRC-03
must_haves:
  truths:
    - "Precheck surfaces traceability drift failures without blocking (exit 0) while preserving checklist output."
    - "Phase and milestone completion paths fail closed when traceability drift exists."
    - "Blocked and passed traceability gate outcomes are recorded deterministically with command, scope, and touched IDs."
  artifacts:
    - path: "scripts/quick_bootstrap.py"
      provides: "Traceability precheck/complete wiring and fail-closed transition sequencing."
      exports: ["main", "record_traceability_gate_activity"]
    - path: "scripts/governance_gates.py"
      provides: "Formatter for grouped traceability failures with explicit retry guidance."
      exports: ["format_traceability_drift_failures"]
    - path: "tests/unit/test_quick_bootstrap.py"
      provides: "CLI routing assertions for non-blocking precheck and fail-closed completion behavior."
      contains: "test_phase_complete_aborts_when_traceability_gate_fails"
    - path: ".planning/STATE.md"
      provides: "Operational traceability gate evidence table for audits."
      contains: "## Traceability Gate Activity"
  key_links:
    - from: "scripts/quick_bootstrap.py"
      to: "scripts/governance_gates.py"
      via: "Single evaluator invocation for precheck and completion commands"
      pattern: "evaluate_traceability_drift_gate|format_traceability_drift_failures"
    - from: "scripts/quick_bootstrap.py"
      to: ".planning/STATE.md"
      via: "Evidence logging for command/scope/result/touched_requirement_ids"
      pattern: "traceability precheck|phase complete|milestone complete"
    - from: "tests/unit/test_quick_bootstrap.py"
      to: "scripts/quick_bootstrap.py"
      via: "Asserts gating order and no delegate call on failure"
      pattern: "requirements gate -> traceability gate -> milestone gate"
---

<objective>
Wire traceability drift enforcement into quick transition boundaries so drift is visible in precheck and blocks completion paths fail-closed.

Purpose: Deliver TRC-02/TRC-03 operational behavior by reusing the shared evaluator from plan 04-01.
Output: `quick_bootstrap` command flow with deterministic traceability evidence logging and retry output.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-traceability-and-drift-enforcement/04-CONTEXT.md
@.planning/phases/04-traceability-and-drift-enforcement/04-RESEARCH.md
@.planning/phases/04-traceability-and-drift-enforcement/04-01-PLAN.md
@scripts/governance_gates.py
@scripts/quick_bootstrap.py
@tests/unit/test_quick_bootstrap.py

<interfaces>
From `scripts/governance_gates.py` (Plan 04-01 output):

```python
def evaluate_traceability_drift_gate(
    planning_root: Path,
    *,
    scope: str = "all",
    touched_requirement_ids: set[str] | None = None,
) -> dict[str, Any]: ...
```

Command surfaces to wire in this plan:

```bash
python scripts/quick_bootstrap.py traceability precheck
python scripts/quick_bootstrap.py phase complete --requirement-id TRC-01
python scripts/quick_bootstrap.py milestone complete v1.1
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add traceability precheck command with non-blocking structured output</name>
  <files>scripts/quick_bootstrap.py, tests/unit/test_quick_bootstrap.py, .planning/STATE.md</files>
  <behavior>
    - Test 1: `traceability precheck` returns JSON with `passed`, `failure_groups`, `retry_command`, and checklist.
    - Test 2: failing precheck exits with code 0 (readiness mode) and records blocked evidence.
    - Test 3: formatter output includes grouped remediation and explicit retry command.
  </behavior>
  <action>Extend CLI args with `traceability precheck` and call `evaluate_traceability_drift_gate(scope="all")`. Mirror existing governance/requirements precheck style: non-blocking return, structured JSON payload, deterministic checklist text, and state evidence write for pass/blocked.</action>
  <verify>
    <automated>pytest -q tests/unit/test_quick_bootstrap.py -k "traceability_precheck or traceability and non_blocking" -x</automated>
  </verify>
  <done>Operators can run drift readiness checks without triggering blocked process exits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce traceability gate in phase/milestone completion with deterministic touched scope</name>
  <files>scripts/quick_bootstrap.py, tests/unit/test_quick_bootstrap.py, .planning/STATE.md</files>
  <behavior>
    - Test 1: `phase complete` calls traceability gate with `scope=touched` and rejects empty/unknown touched IDs.
    - Test 2: `milestone complete` evaluates requirements gate first, then traceability gate, then milestone governance gate.
    - Test 3: any traceability failure aborts delegate invocation and emits one context-specific retry command.
  </behavior>
  <action>Insert traceability gate checks into completion paths after requirements contract validation and before transition delegate/milestone governance gate. For phase completion, pass sorted touched requirement IDs and reject missing IDs as hard failures. Keep flow read-only on block paths except gate evidence logging.</action>
  <verify>
    <automated>pytest -q tests/unit/test_quick_bootstrap.py -k "traceability and (phase_complete or milestone_complete or ordering)" -x</automated>
  </verify>
  <done>Completion commands are fail-closed on traceability drift and preserve deterministic gate ordering.</done>
</task>

</tasks>

<verification>
- Run `pytest -q tests/unit/test_quick_bootstrap.py -k "traceability" -x`.
- Verify blocked completion paths do not call `run_transition_delegate`.
- Verify state evidence captures scope and sorted touched IDs for successful/blocked checks.
</verification>

<success_criteria>
- TRC-03 is enforced by blocking phase completion when traceability links are missing/inconsistent.
- TRC-02 is operationalized in precheck and completion commands with deterministic diagnostics.
- Command flow remains consistent with existing fail-closed governance style.
</success_criteria>

<output>
After completion, create `.planning/phases/04-traceability-and-drift-enforcement/04-02-SUMMARY.md`
</output>
