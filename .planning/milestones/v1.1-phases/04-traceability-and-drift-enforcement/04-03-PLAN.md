---
phase: 04-traceability-and-drift-enforcement
plan: 3
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - tests/integration/test_milestone_governance_flow.py
  - tests/unit/test_governance_gates.py
  - tests/unit/test_quick_bootstrap.py
  - .planning/STATE.md
autonomous: true
requirements:
  - TRC-01
  - TRC-02
  - TRC-03
must_haves:
  truths:
    - "Cross-artifact drift (requirements status vs mapped roadmap phase completion) is detected as a hard error."
    - "Failure group rendering order for traceability remains stable across unit and integration test suites."
    - "Milestone/phase completion continues to avoid false advancement when traceability gate blocks."
  artifacts:
    - path: "tests/unit/test_governance_gates.py"
      provides: "State-vs-mapping drift and deterministic ordering coverage for traceability evaluator."
      contains: "test_traceability_gate_detects_state_mapping_mismatch"
    - path: "tests/unit/test_quick_bootstrap.py"
      provides: "CLI-level regression tests for traceability retry, no-delegate blocking, and touched-scope evidence."
      contains: "test_milestone_complete_aborts_when_traceability_gate_fails_after_requirements_pass"
    - path: "tests/integration/test_milestone_governance_flow.py"
      provides: "End-to-end enforcement order and no-false-completion guarantees with traceability in the chain."
      contains: "test_milestone_complete_blocks_on_traceability_drift_before_milestone_gate"
    - path: ".planning/STATE.md"
      provides: "Persisted gate activity evidence validated by tests for blocked/passed transitions."
      contains: "## Traceability Gate Activity"
  key_links:
    - from: "tests/integration/test_milestone_governance_flow.py"
      to: "scripts/quick_bootstrap.py"
      via: "Asserts requirements->traceability->milestone gate execution order"
      pattern: "complete|blocked|passed"
    - from: "tests/unit/test_governance_gates.py"
      to: ".planning/ROADMAP.md"
      via: "Fixture-driven phase completion mismatch checks"
      pattern: "state_mapping_mismatch"
    - from: "tests/unit/test_quick_bootstrap.py"
      to: ".planning/STATE.md"
      via: "Evidence row assertions for deterministic gate activity records"
      pattern: "| Date | Command | Scope | Result |"
---

<objective>
Close traceability drift coverage gaps across unit and integration suites, including state-vs-mapping mismatches and transition-order regressions.

Purpose: Prove the phase goal end-to-end and lock regression boundaries around TRC-01/TRC-02/TRC-03.
Output: Deterministic test coverage that validates failure taxonomy, ordering, and fail-closed transition behavior.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-traceability-and-drift-enforcement/04-CONTEXT.md
@.planning/phases/04-traceability-and-drift-enforcement/04-RESEARCH.md
@.planning/phases/04-traceability-and-drift-enforcement/04-02-PLAN.md
@tests/unit/test_governance_gates.py
@tests/unit/test_quick_bootstrap.py
@tests/integration/test_milestone_governance_flow.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add evaluator tests for state-mapping mismatches and fixed failure group ordering</name>
  <files>tests/unit/test_governance_gates.py</files>
  <behavior>
    - Test 1: requirement status `Complete` mapped to incomplete roadmap phase fails with `state_mapping_mismatch`.
    - Test 2: mapped phase complete while requirement not `Complete` fails with `state_mapping_mismatch`.
    - Test 3: full multi-error scenario returns canonical group order for traceability failures.
  </behavior>
  <action>Create focused unit fixtures that vary roadmap checkbox completion and requirement status values, then assert exact failure group keys and remediation presence. Keep assertions strict on deterministic order and envelope shape.</action>
  <verify>
    <automated>pytest -q tests/unit/test_governance_gates.py -k "traceability and (state_mapping_mismatch or ordering)" -x</automated>
  </verify>
  <done>TRC-02 drift semantics are locked by deterministic evaluator tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add transition-level integration tests for requirements->traceability->milestone ordering</name>
  <files>tests/integration/test_milestone_governance_flow.py, tests/unit/test_quick_bootstrap.py</files>
  <behavior>
    - Test 1: milestone completion blocks on traceability drift after requirements pass and before milestone governance gate.
    - Test 2: blocked traceability path records only blocked evidence and preserves existing completion status.
    - Test 3: retry command remains command-specific (`milestone complete` or `phase complete`) and appears once.
  </behavior>
  <action>Extend integration and unit flows to cover ordering and fail-closed behavior with traceability gate inserted. Use monkeypatch spies to assert that milestone governance gate is not invoked when traceability fails, and delegate invocation remains suppressed on blocked paths.</action>
  <verify>
    <automated>pytest -q tests/unit/test_quick_bootstrap.py -k "traceability and (retry or no_delegate or ordering)" -x && pytest -q tests/integration/test_milestone_governance_flow.py -k "traceability" -x</automated>
  </verify>
  <done>Transition ordering and blocking guarantees are covered end-to-end.</done>
</task>

</tasks>

<verification>
- Run `pytest -q tests/unit/test_governance_gates.py -k "traceability" -x`.
- Run `pytest -q tests/unit/test_quick_bootstrap.py -k "traceability" -x`.
- Run `pytest -q tests/integration/test_milestone_governance_flow.py -k "traceability" -x`.
- Run focused smoke suite: `pytest -q tests/unit/test_quick_bootstrap.py tests/integration/test_milestone_governance_flow.py -k "phase_complete or milestone_complete" -x`.
</verification>

<success_criteria>
- TRC-01, TRC-02, and TRC-03 have explicit regression coverage tied to failure taxonomy and transition sequencing.
- Deterministic failure rendering order is validated in both evaluator and command-level outputs.
- No false advancement regressions remain in completion flows when drift exists.
</success_criteria>

<output>
After completion, create `.planning/phases/04-traceability-and-drift-enforcement/04-03-SUMMARY.md`
</output>
