---
phase: 03-requirements-contract-enforcement
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/governance_gates.py
  - tests/unit/test_governance_gates.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - REQ-01
  - REQ-02
must_haves:
  truths:
    - "El validador bloquea de forma determinista IDs que no cumplen `CAT-NN` (categoria uppercase + sufijo numerico >=2)."
    - "Cada requirement activo exige `id`, `status`, y `acceptance_criteria` no vacio y verificable."
    - "El contrato aplica de forma estricta a secciones `v1`, `v2` y `Out of Scope`."
  artifacts:
    - path: "scripts/governance_gates.py"
      provides: "Evaluador central del contrato de REQUIREMENTS con taxonomia determinista de fallos."
      exports: ["evaluate_requirements_contract_gate", "format_requirements_contract_failures"]
    - path: "tests/unit/test_governance_gates.py"
      provides: "Cobertura unitaria para formato de ID, unicidad, status permitido y acceptance criteria."
      contains: "test_requirements_gate_fails_on_invalid_id_format"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Contrato canonico con campos explicitos por requirement (id/status/acceptance_criteria)."
      contains: "acceptance_criteria:"
  key_links:
    - from: "scripts/governance_gates.py"
      to: ".planning/REQUIREMENTS.md"
      via: "parseo lineal de secciones + validaciones de contrato"
      pattern: "CAT-[0-9]{2,}|Pending|In Progress|Complete|Blocked"
    - from: "tests/unit/test_governance_gates.py"
      to: "scripts/governance_gates.py"
      via: "assert de orden y codigos de failure_groups"
      pattern: "invalid_id_format|duplicate_id|invalid_status|missing_acceptance_criteria"
---

<objective>
Definir y validar el contrato canonico de `REQUIREMENTS.md` para que IDs y campos requeridos sean fail-closed y deterministas.

Purpose: Cumplir REQ-01/REQ-02 con una sola implementacion reusable antes de cablear transiciones.
Output: Evaluador de contrato en `governance_gates.py`, pruebas unitarias, y migracion del formato de `REQUIREMENTS.md`.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-requirements-contract-enforcement/03-CONTEXT.md
@.planning/phases/03-requirements-contract-enforcement/03-RESEARCH.md
@scripts/governance_gates.py
@tests/unit/test_governance_gates.py

<interfaces>
From `scripts/governance_gates.py` (existing):

```python
def evaluate_milestone_governance_gate(planning_root: Path, milestone_version: str) -> dict[str, Any]: ...
def format_gate_failures(result: dict[str, Any], retry_command: str) -> str: ...
```

New contracts to add in this plan:

```python
def evaluate_requirements_contract_gate(
    planning_root: Path,
    *,
    scope: str = "all",
    touched_requirement_ids: set[str] | None = None,
) -> dict[str, Any]: ...

def format_requirements_contract_failures(
    result: dict[str, Any],
    retry_command: str,
) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce explicit requirements-entry schema and deterministic parser tests</name>
  <files>.planning/REQUIREMENTS.md, tests/unit/test_governance_gates.py</files>
  <behavior>
    - Test 1: cada requirement activo en `v1`, `v2` y `Out of Scope` expone campos parseables `id`, `status`, `acceptance_criteria`.
    - Test 2: IDs fuera de `^[A-Z]+-[0-9]{2,}$` fallan con `invalid_id_format`.
    - Test 3: IDs duplicados en requirements activos fallan con `duplicate_id`.
  </behavior>
  <action>Migrar `.planning/REQUIREMENTS.md` a una forma canonica por requirement con campos explicitos (`id`, `status`, `acceptance_criteria`) manteniendo el contenido funcional existente. Antes de implementar el evaluador, crear/actualizar pruebas en `tests/unit/test_governance_gates.py` para fijar parseo de entradas, validacion de formato `CAT-NN`, unicidad global y orden determinista de grupos de fallo (`missing_file`, `malformed_entry`, `invalid_id_format`, `duplicate_id`, `invalid_status`, `missing_acceptance_criteria`).</action>
  <verify>
    <automated>pytest -q tests/unit/test_governance_gates.py -k "requirements and (id_format or duplicate or malformed_entry)" -x</automated>
  </verify>
  <done>Existe un contrato markdown canonico y pruebas RED/GREEN que fijan reglas de ID y parseo estructural en todas las secciones.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared requirements contract gate and remediation formatter</name>
  <files>scripts/governance_gates.py, tests/unit/test_governance_gates.py</files>
  <behavior>
    - Test 1: status fuera de `Pending|In Progress|Complete|Blocked` falla con `invalid_status`.
    - Test 2: `acceptance_criteria` vacio o ausente falla con `missing_acceptance_criteria`.
    - Test 3: evaluaciones exitosas retornan `passed=true` y envelope estable (`passed`, `failure_groups`, `retry_command`, `requirements_file`).
  </behavior>
  <action>Agregar `evaluate_requirements_contract_gate` en `scripts/governance_gates.py` reutilizando el patron fail-closed ya existente. Aplicar reglas bloqueadas del contexto: IDs `CAT-NN`, unicidad global para activos v1, statuses finitos exactos, acceptance criteria no vacio/testable mediante chequeos estructurales deterministas (sin heuristicas ambiguas), y schema estricto en todas las secciones. Implementar `format_requirements_contract_failures` con checklist agrupado por tipo y remediation concreta; mantener comportamiento read-only (sin mutaciones de `.planning/*` en rutas de fallo).</action>
  <verify>
    <automated>pytest -q tests/unit/test_governance_gates.py -k "requirements and (status or acceptance or formatter or passed)" -x</automated>
  </verify>
  <done>El gate de contrato de requirements queda centralizado, determinista y listo para ser invocado por rutas de precheck y transicion.</done>
</task>

</tasks>

<verification>
- Ejecutar `pytest -q tests/unit/test_governance_gates.py -k "requirements" -x`.
- Confirmar que `evaluate_requirements_contract_gate` no escribe archivos y solo devuelve diagnostico.
- Verificar que la taxonomia de fallos y su orden se mantiene estable entre ejecuciones.
</verification>

<success_criteria>
- REQ-01 queda cubierto con validacion estricta de IDs estables y unicidad.
- REQ-02 queda cubierto con verificacion obligatoria de status y acceptance criteria por requirement activo.
- Existe un evaluador y formatter unicos para reutilizacion en comandos de transicion.
</success_criteria>

<output>
After completion, create `.planning/phases/03-requirements-contract-enforcement/03-01-SUMMARY.md`
</output>
