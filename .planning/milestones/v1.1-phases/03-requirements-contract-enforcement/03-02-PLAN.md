---
phase: 03-requirements-contract-enforcement
plan: 2
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - scripts/quick_bootstrap.py
  - scripts/governance_gates.py
  - tests/unit/test_quick_bootstrap.py
  - tests/integration/test_milestone_governance_flow.py
  - .planning/STATE.md
autonomous: true
requirements:
  - REQ-03
must_haves:
  truths:
    - "El operador dispone de un `requirements precheck` no bloqueante que usa el mismo validador canonico."
    - "Roadmap create/revise y milestone complete se bloquean fail-closed cuando el contrato de requirements no pasa."
    - "En fallo, los comandos no avanzan estado/roadmap ni registran cierres falsos (solo evidencia de gate bloqueado)."
  artifacts:
    - path: "scripts/quick_bootstrap.py"
      provides: "Rutas CLI para precheck de requirements y enforcement en transiciones."
      exports: ["parse_args", "main"]
    - path: "tests/unit/test_quick_bootstrap.py"
      provides: "Cobertura de routing y bloqueo de transiciones por requirements gate."
      contains: "test_requirements_precheck_reports_structured_non_blocking_result"
    - path: "tests/integration/test_milestone_governance_flow.py"
      provides: "Flujos integrados que prueban abort-before-mutation en completion."
      contains: "test_milestone_complete_aborts_when_requirements_contract_gate_fails"
    - path: ".planning/STATE.md"
      provides: "Evidencia de intentos de gate sin advancement espurio."
      contains: "gate blocked"
  key_links:
    - from: "scripts/quick_bootstrap.py"
      to: "scripts/governance_gates.py"
      via: "invocacion unica de evaluate_requirements_contract_gate antes de transiciones"
      pattern: "requirements precheck|roadmap|milestone complete|phase complete"
    - from: "scripts/quick_bootstrap.py"
      to: ".planning/STATE.md"
      via: "registro de evidencia operacional en bloqueos sin cambiar completion status"
      pattern: "record_.*gate_activity|blocked|passed"
    - from: "tests/unit/test_quick_bootstrap.py"
      to: "scripts/quick_bootstrap.py"
      via: "assert de no-mutation cuando gate falla"
      pattern: "returncode|blocked|retry"
---

<objective>
Cablear enforcement del contrato de requirements en prechecks y transiciones criticas, manteniendo fail-closed y zero-mutation en fallo.

Purpose: Cumplir REQ-03 en fronteras reales de operacion (roadmap y milestone transitions).
Output: CLI con `requirements precheck`, bloqueos de transicion reutilizando evaluador comun, y pruebas unitarias/integracion de no-advancement.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-requirements-contract-enforcement/03-CONTEXT.md
@.planning/phases/03-requirements-contract-enforcement/03-RESEARCH.md
@.planning/phases/03-requirements-contract-enforcement/03-01-PLAN.md
@scripts/quick_bootstrap.py
@scripts/governance_gates.py

<interfaces>
From `scripts/quick_bootstrap.py`:

```python
def parse_args() -> argparse.Namespace: ...
def main() -> int: ...
```

From `scripts/governance_gates.py` (Plan 03-01 output):

```python
def evaluate_requirements_contract_gate(
    planning_root: Path,
    *,
    scope: str = "all",
    touched_requirement_ids: set[str] | None = None,
) -> dict[str, Any]: ...
def format_requirements_contract_failures(result: dict[str, Any], retry_command: str) -> str: ...
```

CLI surface to enforce in this plan:

```bash
python scripts/quick_bootstrap.py requirements precheck
python scripts/quick_bootstrap.py milestone complete v1.1
python scripts/quick_bootstrap.py roadmap create
python scripts/quick_bootstrap.py roadmap revise
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add requirements precheck command with deterministic structured diagnostics</name>
  <files>scripts/quick_bootstrap.py, tests/unit/test_quick_bootstrap.py, .planning/STATE.md</files>
  <behavior>
    - Test 1: `requirements precheck` devuelve JSON no bloqueante con `passed`, `failure_groups`, `retry_command`, y checklist.
    - Test 2: salida de diagnostico usa el formatter compartido del gate de requirements.
    - Test 3: se registra evidencia de `precheck` en `STATE.md` sin marcar completion.
  </behavior>
  <action>Extender `parse_args`/`main` para agregar `requirements precheck` usando exclusivamente `evaluate_requirements_contract_gate` y `format_requirements_contract_failures`. Mantener semantica no bloqueante (exit code 0) para readiness checks y salida determinista machine-readable. Registrar actividad del gate en `STATE.md` con resultado passed/blocked sin mutar otros estados de milestone o roadmap.</action>
  <verify>
    <automated>pytest -q tests/unit/test_quick_bootstrap.py -k "requirements_precheck or requirements and retry" -x</automated>
  </verify>
  <done>Existe un precheck explicito y reutilizable para requirements, con diagnostico determinista y evidencia operacional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce requirements gate before roadmap create/revise and milestone complete</name>
  <files>scripts/quick_bootstrap.py, tests/unit/test_quick_bootstrap.py, tests/integration/test_milestone_governance_flow.py, .planning/STATE.md</files>
  <behavior>
    - Test 1: `milestone complete` aborta con exit code 1 cuando requirements gate falla, sin mutar cierre.
    - Test 2: `roadmap create/revise` aborta fail-closed cuando requirements gate falla.
    - Test 3: si gate pasa, los comandos delegan al flujo existente y registran evidencia correcta.
  </behavior>
  <action>Antes de ejecutar transiciones (`roadmap create`, `roadmap revise`, `milestone complete`), invocar el gate de requirements y cortar ejecucion en fallo mostrando checklist+retry contextual. Si en el workspace no existe comando local de roadmap, agregar wrappers de transicion en `quick_bootstrap.py` que deleguen al comando real solo tras gate exitoso; no duplicar logica del validador. En rutas de fallo, garantizar read-only: no escrituras de avance en roadmap/state mas alla de evidencia de gate bloqueado.</action>
  <verify>
    <automated>pytest -q tests/unit/test_quick_bootstrap.py -k "requirements_gate and (roadmap or milestone_complete)" -x && pytest -q tests/integration/test_milestone_governance_flow.py -k "requirements and blocked" -x</automated>
  </verify>
  <done>Las fronteras de roadmap y milestone transitions quedan protegidas por el mismo gate de requirements y no avanzan en fallo.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce touched-requirements validation on phase completion path</name>
  <files>scripts/quick_bootstrap.py, scripts/governance_gates.py, tests/unit/test_quick_bootstrap.py</files>
  <action>Agregar ruta de `phase complete` (o hook equivalente existente) que acepte set de requirement IDs tocados y ejecute `evaluate_requirements_contract_gate(..., scope=\"touched\", touched_requirement_ids=...)` antes de cerrar fase. Si el gate falla, abortar con checklist y comando de reintento; si pasa, continuar flujo de cierre. Mantener fuera de alcance validaciones de drift cross-artifact (Phase 4).</action>
  <verify>
    <automated>pytest -q tests/unit/test_quick_bootstrap.py -k "phase_complete and touched_requirements" -x</automated>
  </verify>
  <done>El cierre de fase valida contrato en los requirements tocados y bloquea completion cuando hay incumplimiento.</done>
</task>

</tasks>

<verification>
- Ejecutar `pytest -q tests/unit/test_quick_bootstrap.py -k "requirements" -x`.
- Ejecutar `pytest -q tests/integration/test_milestone_governance_flow.py -k "requirements or blocked" -x`.
- Verificar manualmente que comandos bloqueados muestran un solo retry command contextual y no crean advancement writes.
</verification>

<success_criteria>
- REQ-03 queda cubierto con enforcement previo a roadmap transitions y milestone completion.
- Existe ruta `requirements precheck` no bloqueante para readiness operativa.
- Fallos del gate son read-only y no introducen cierres o avances falsos.
</success_criteria>

<output>
After completion, create `.planning/phases/03-requirements-contract-enforcement/03-02-SUMMARY.md`
</output>
