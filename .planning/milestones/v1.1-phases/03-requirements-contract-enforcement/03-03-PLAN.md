---
phase: 03-requirements-contract-enforcement
plan: 3
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - tests/unit/test_quick_bootstrap.py
autonomous: true
gap_closure: true
requirements:
  - REQ-03
must_haves:
  truths:
    - "The milestone-complete short-circuit test asserts requirements-gate evidence when requirements validation fails first."
    - "The grouped milestone-failures test reaches milestone gate formatting by satisfying requirements-gate preconditions."
    - "Both previously failing unit tests are green in targeted and paired test runs."
  artifacts:
    - path: "tests/unit/test_quick_bootstrap.py"
      provides: "Aligned milestone-complete unit scenarios for requirements-first enforcement ordering."
      contains: "test_milestone_complete_aborts_without_false_completion_on_gate_failure"
  key_links:
    - from: "tests/unit/test_quick_bootstrap.py"
      to: "scripts/quick_bootstrap.py"
      via: "requirements gate short-circuit branch in milestone complete flow"
      pattern: "evaluate_requirements_contract_gate|record_requirements_gate_activity"
    - from: "tests/unit/test_quick_bootstrap.py"
      to: ".planning/REQUIREMENTS.md"
      via: "fixture preconditions that allow milestone gate branch coverage"
      pattern: "id: REQ-01|status: Pending|acceptance_criteria:"
---

<objective>
Close Phase 3 verification gaps by aligning milestone-complete unit expectations with requirements-first gate ordering and by adding requirements-valid fixtures where milestone grouped-failure output is asserted.

Purpose: Restore a green, behavior-accurate test surface for REQ-03 enforcement without replanning delivered implementation work.
Output: Targeted test updates in `tests/unit/test_quick_bootstrap.py` and deterministic verification commands for the two blocker scenarios.
</objective>

<execution_context>
@/Users/seifreed/.codex/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-requirements-contract-enforcement/03-02-PLAN.md
@.planning/phases/03-requirements-contract-enforcement/03-VERIFICATION.md
@scripts/quick_bootstrap.py
@tests/unit/test_quick_bootstrap.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix requirements-first short-circuit expectations in milestone-complete gate-failure test</name>
  <files>tests/unit/test_quick_bootstrap.py</files>
  <behavior>
    - Test 1: when requirements gate fails first, output assertions verify requirements retry/checklist behavior, not milestone formatter output.
    - Test 2: state assertions verify requirements gate activity entries (`scope=all`, blocked) and absence of false pass entries.
  </behavior>
  <action>Update `test_milestone_complete_aborts_without_false_completion_on_gate_failure` so fixtures and assertions target the actual branch at `quick_bootstrap.main()` where requirements validation short-circuits milestone completion. Keep the test intent (fail-closed + no false completion) but assert requirements-gate evidence instead of milestone-gate evidence.</action>
  <verify>
    <automated>pytest -q tests/unit/test_quick_bootstrap.py::test_milestone_complete_aborts_without_false_completion_on_gate_failure -x</automated>
  </verify>
  <done>Short-circuit unit test reflects requirements-first ordering and passes deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed valid requirements fixture so grouped milestone-failure formatting is exercised</name>
  <files>tests/unit/test_quick_bootstrap.py</files>
  <behavior>
    - Test 1: grouped milestone failure assertions execute after requirements gate passes.
    - Test 2: output still includes grouped remediation lines (`missing_file`, `invalid_status`, `stale_audit`, `malformed_sections`).
  </behavior>
  <action>Update `test_milestone_complete_grouped_failures_output_has_grouped_failures_and_remediation` to create a minimal valid `.planning/REQUIREMENTS.md` fixture (or equivalent monkeypatch) so milestone-complete flow reaches `evaluate_milestone_governance_gate`. Preserve grouped-remediation assertions and ensure no requirement-contract failure masks this scenario.</action>
  <verify>
    <automated>pytest -q tests/unit/test_quick_bootstrap.py::test_milestone_complete_grouped_failures_output_has_grouped_failures_and_remediation -x</automated>
  </verify>
  <done>Grouped-failure milestone test validates milestone formatter behavior under valid requirements preconditions and passes.</done>
</task>

</tasks>

<verification>
- Execute `pytest -q tests/unit/test_quick_bootstrap.py::test_milestone_complete_aborts_without_false_completion_on_gate_failure tests/unit/test_quick_bootstrap.py::test_milestone_complete_grouped_failures_output_has_grouped_failures_and_remediation -x`.
- Execute `pytest -q tests/unit/test_quick_bootstrap.py -k "milestone_complete and (gate_failure or grouped_failures_output)" -x`.
</verification>

<success_criteria>
- Both gaps reported in `03-VERIFICATION.md` are closed by passing tests at lines previously failing (`306`, `352` references).
- Requirements-first milestone-complete flow remains explicitly covered by assertions aligned to the current control path.
- Milestone grouped-failure remediation output remains covered with requirements preconditions satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/03-requirements-contract-enforcement/03-03-SUMMARY.md`
</output>
